# 1장. 주요 언어 모델 공급자 및 활용

- google gemini API
- groq
- ollama

# 2장. RAG 답변 평가

## 휴리스틱 평가 - 정량적인 평가

## ROUGE, BLEU 평가

- ROUGE (Recall-Oriented Understudy for Gisting Evaluation)

```
    from rouge_score import rouge_scorer
```

- BLEU (Bilingual Evaluation Understudy)

```
    from nltk.translate.bleu_score import sentence_bleu, SmoothingFunction
```

- 문자열 및 임베딩 거리 평가

## OpenEvals를 활용한 LLM 평가

LangChain에서 제공하는 **공식 평가 라이브러리**입니다.

```
from openevals.prompts import (
    CONCISENESS_PROMPT,   # 간결성 평가
    CORRECTNESS_PROMPT,   # 정확성 평가 (참조 답변 필요)
    RAG_GROUNDEDNESS_PROMPT,   # 컨텍스트 기반 신뢰성 평가
    RAG_HELPFULNESS_PROMPT,   # 유용성 평가
    RAG_RETRIEVAL_RELEVANCE_PROMPT   # 검색 관련성 평가
)

conciseness_evaluator_kr = create_llm_as_judge(
    prompt=CONCISENESS_PROMPT_KR,
    feedback_key="conciseness",
    model="openai:gpt-4.1-mini",
)

# 샘플에 대해 평가 수행
conciseness_result = conciseness_evaluator_kr(
    input=query,    # 질문
    output=answer,     # 평가 대상: LLM 모델의 예측
)

```

# 3장. LLM을 활용한 성능 평가 (LLM-as-Judge)

## LLM-as-Judge

- Reference-free 평가
- Reference-based 평가

## 점수 척도 평가

# 4장. LLM 성능평가 - Pairwise (비교평가) A/B 테스트

2개의 답변을 비교평가

```
from openevals.llm import create_llm_as_judge

# 비교 평가기 생성 (choices로 3단계 척도 지정)
pairwise_evaluator = create_llm_as_judge(
    prompt=PAIRWISE_COMPARISON_PROMPT,
    feedback_key="pairwise_comparison",
    choices=[0.0, 0.5, 1.0],   # 0=A 승, 0.5=TIE, 1=B 승
    model="openai:gpt-4.1-mini",
)

# 두 모델의 출력 비교
result = pairwise_evaluator(
    input="파이썬이 무엇인지 설명해주세요.",
    prediction="파이썬은 읽기 쉽고 간단한 문법을 가진 프로그래밍 언어입니다.",
    prediction_b="파이썬은 동적 타이핑을 지원하는 고수준 프로그래밍 언어로, 데이터 과학과 웹 개발에 널리 사용됩니다.",
)

```

# 5장. Langfuse Evaluation을 사용한 RAG 답변 평가

```
from langfuse import get_client, Evaluation

# Langfuse 클라이언트 초기화
langfuse_client = get_client()

# Experiment Runner SDK를 사용한 평가 실행
dataset = langfuse_client.get_dataset("RAG_Evaluation_Dataset_Test")

# 실험 실행
result = dataset.run_experiment(
    name="RAG_Evaluation_Experiment",
    description="ROUGE와 간결성을 활용한 RAG 시스템 평가",
    task=rag_task,
    evaluators=[
        rouge_evaluator,
        conciseness_llm_evaluator,
        length_evaluator
    ],
    run_evaluators=[
        average_rouge_evaluator,
        average_conciseness_evaluator
    ],
    max_concurrency=3,  # 동시 실행 수 제한
)

```

# 6장. 평가자 일치도 분석

여러 LLM-as-Judge의 결과 일치도를 정량화

## Cohens Kappa

(분자 - 우연일치확률) / (분모 - 우연일치확률) ==> 0에 가까울수록 우연히 맞음. 평가모델을 믿을 수 없음.

```
from sklearn.metrics import cohen_kappa_score
kappa = cohen_kappa_score(labels_a, labels_b)
```

# 요약정리

Retriever 평가
→ ranx

RAG 평가
→ ragas

답변 비교
→ pairwise evaluation

평가 일관성
→ Cohen's kappa
